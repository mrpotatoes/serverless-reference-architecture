{
  "name": "@serverless-reference-architecture",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "lerna run start",
    "build": "lerna run build",
    "bootstrap": "yarn && lerna bootstrap && lerna exec -- yarn && yarn build:shared"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}